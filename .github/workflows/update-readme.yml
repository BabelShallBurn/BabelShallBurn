name: Update Profile README

permissions:
  contents: write

on:
  schedule:
    - cron: "0 7 * * *"   # täglich 07:00 UTC
  workflow_dispatch:      # manuell startbar

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Update README
        run: |
          echo "## 👋 Hi, I'm Eric" > README.md
          echo "" >> README.md
          echo "🚀 Learning AI Engineering" >> README.md
          echo "🧠 Focus: Python, FastAPI, LangChain" >> README.md
          echo "" >> README.md
          echo "⏱ Last update: $(date -u)" >> README.md

      - name: Commit and push changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add README.md
          git commit -m "Automated README update" || exit 0
          git push